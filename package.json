{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "homepage": "https://github.com/zhekai3322/ThinkAble.git",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
